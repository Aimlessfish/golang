# Discord Bot Configuration
DISCORD_BOT_TOKEN=
# Steam Session Configuration
SESSION_START_PORT=8080
SESSION_TIMEOUT_MINUTES=30

# Credential Storage
# Credentials will be stored in steam_credentials.json (auto-created)
# You can also add credentials via CLI: cred-add <account> <password> [shared_secret]

# Optional: Browser Configuration
HEADLESS_BROWSER=false

# Optional: API Configuration
CONTROL_API_PORT=9000
